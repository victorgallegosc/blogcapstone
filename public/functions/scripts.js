function getPostData(postData) {
    var post = JSON.parse(postData);
    console.log("Post data:", post);
    // var firstName = post.fname;
    // var lastName = post.lname;
    // var postText = post.ptext;

    // document.getElementsByName("fname").setAttribute('value') = firstName;
    // document.getElementsByName("lname").setAttribute('value') = lastName;
    // document.getElementsByName("ptext").setAttribute('value') = postText;
    // document.getElementById("postForm").setAttribute('action') = '/edit'

    // postToEdit = post;
    // window.scrollTo(0,0);
}